<% provide(:title, 'Sign up') %>
<h1>Регистрация пользователя:</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Создать аккаунт", class: "btn-large btn-primary" %>
    <% end %>
  </div>
</div>